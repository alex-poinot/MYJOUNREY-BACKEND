# Configuration pour l'environnement staging
NODE_ENV=staging
PORT=3000
HTTPS_PORT=3443

# Configuration HTTPS (optionnel)
HTTPS_ENABLED=false
SSL_KEY_PATH=./certs/private-key.pem
SSL_CERT_PATH=./certs/certificate.pem

# Configuration de la base de données
DB_HOST=RCT-SQL-11\ISAE3402
DB_PORT=1433
DB_USER=USR_APP_TRANSFORM
DB_PASSWORD=transformjava
DB_NAME=Transform_Test

# Timeouts de base de données (en millisecondes)
DB_REQUEST_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=30000

# Configuration du pool de connexions
DB_POOL_MAX=10
DB_POOL_MIN=0

# Configuration des logs
LOG_LEVEL=info
LOG_MAX_SIZE=20m
LOG_MAX_FILES=14d

# Configuration CORS
CORS_ORIGIN=*